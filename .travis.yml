language: rust

cache: cargo

rust:
  - nightly

before_install:
  - (cargo +nightly install rustfmt-nightly || true)

script:
  - cargo fmt -- --check
  - cargo build --verbose --all
  - cargo test --verbose --all
